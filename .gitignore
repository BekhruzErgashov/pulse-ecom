node_modules/
.next/
.env
.env.local
.env.docker.local
npm-debug.log*
.vercel
.env*
# .env* выше прячет и шаблон — его как раз нужно держать в репозитории.
!.env.example
# .env.docker секретов не содержит (только NODE_ENV), но на него ссылается
# docker-compose.yml — без него `npm run docker:dev` после клона не поднимется.
!.env.docker
# Настройки IDE (PyCharm/WebStorm) — личные, в репозитории не нужны.
.idea/
# То же для Claude Code: настройки прав и конфиг дев-сервера — локальные.
.claude/
