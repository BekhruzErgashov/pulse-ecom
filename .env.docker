# Не используется текущим кодом (данные хранятся в памяти, см. lib/store.ts),
# оставлено для совместимости с docker-compose.yml и на случай подключения
# настоящей DynamoDB/Yandex Cloud Document API в будущем.
NODE_ENV=development
