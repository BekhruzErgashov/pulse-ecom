"use client";

/**
 * В оригинальном шаблоне vibecraft этот компонент слушает handshake-сообщения
 * от редактора платформы SourceCraft. В автономной сборке (вне платформы)
 * он ничего не делает — оставлен для совместимости, чтобы не ломать
 * структуру шаблона при последующей синхронизации.
 */
export function HandshakeListener() {
  return null;
}
